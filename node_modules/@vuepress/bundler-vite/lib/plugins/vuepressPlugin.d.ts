import type { App } from '@vuepress/core';
import type { Plugin } from 'vite';
export declare const vuepressPlugin: ({ app, isBuild, isServer, }: {
    app: App;
    isBuild: boolean;
    isServer: boolean;
}) => Plugin[];
