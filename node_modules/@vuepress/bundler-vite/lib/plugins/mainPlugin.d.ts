import type { App } from '@vuepress/core';
import type { Plugin } from 'vite';
export declare const mainPlugin: ({ app, isBuild, isServer, }: {
    app: App;
    isBuild: boolean;
    isServer: boolean;
}) => Plugin;
