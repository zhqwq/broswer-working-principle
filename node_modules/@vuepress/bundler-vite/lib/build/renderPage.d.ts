import type { App, Page } from '@vuepress/core';
import type { OutputAsset, OutputChunk, RollupOutput } from 'rollup';
import type { App as VueApp } from 'vue';
import type { Router as VueRouter } from 'vue-router';
export declare const renderPage: ({ app, page, vueApp, vueRouter, ssrTemplate, output, outputEntryChunk, outputCssAsset, }: {
    app: App;
    page: Page;
    vueApp: VueApp;
    vueRouter: VueRouter;
    ssrTemplate: string;
    output: RollupOutput['output'];
    outputEntryChunk: OutputChunk;
    outputCssAsset: OutputAsset | undefined;
}) => Promise<void>;
